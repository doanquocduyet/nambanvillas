name: Daily Nam Ban Listings

on:
  schedule:
    # 00:07 UTC = 07:07 sáng giờ Việt Nam, mỗi ngày
    - cron: '7 0 * * *'
  workflow_dispatch:  # cho phép bấm chạy tay từ tab Actions

jobs:
  post-listings:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Tìm & viết tin rao mới
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: node scripts/daily-listings.mjs

      - name: Rebuild RSS feed
        run: node scripts/build-feed.mjs

      - name: Commit and push if changed
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          # data/tin-rao-state.json chỉ có khi đã thu được tin (cần ANTHROPIC_API_KEY) — thiếu file thì bỏ qua, không làm đỏ cả lần chạy
          git add thi-truong/tin-rao-dat-nam-ban-moi/index.html feed.xml
          [ -f data/tin-rao-state.json ] && git add data/tin-rao-state.json || true
          git diff --staged --quiet || git commit -m "chore: tin rao Nam Ban $(date +'%Y-%m-%d') [skip ci]"
          # Máy khác có thể vừa đẩy — kéo về rồi đẩy, tránh bị chặn
          git pull --rebase --autostash -q origin main || true
          git push
